// Starter script for the Product R&D Tracker.
// We keep this intentionally small in step 1.

const productsRoot = document.getElementById('products-root');

if (productsRoot) {
  productsRoot.textContent =
    'Project scaffold is ready. Next step: load and render product data.';
}
